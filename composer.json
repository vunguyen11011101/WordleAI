{
    "name": "vunguyen/wordle_ai",
    "description": "ai bot that can play wordle",
    "require": {
        "guzzlehttp/guzzle": "^8.0"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "require-dev": {
        "phpunit/phpunit": "^12.5"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },

    "autoload-dev": {
        "classmap": [
            "tests/"
        ]
    },

    "scripts": {
        "test": "phpunit"
    }
}
